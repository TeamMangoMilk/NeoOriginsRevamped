{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://neoorigins.example/schema/action.schema.json",
  "title": "NeoOrigins Action",
  "description": "A side-effect action used in `entity_action` fields. The `type` field discriminates.",
  "type": "object",
  "required": [
    "type"
  ],
  "properties": {
    "type": {
      "type": "string",
      "description": "Fully-qualified action verb. `apace:` aliases are accepted.",
      "enum": [
        "neoorigins:and",
        "neoorigins:chance",
        "neoorigins:delay",
        "neoorigins:if_else",
        "neoorigins:if_else_list",
        "neoorigins:nothing",
        "neoorigins:damage",
        "neoorigins:heal",
        "neoorigins:feed",
        "neoorigins:exhaust",
        "neoorigins:change_resource",
        "neoorigins:apply_effect",
        "neoorigins:clear_effect",
        "neoorigins:add_velocity",
        "neoorigins:launch",
        "neoorigins:set_fall_distance",
        "neoorigins:dismount",
        "neoorigins:give",
        "neoorigins:modify_food",
        "neoorigins:spawn_entity",
        "neoorigins:spawn_projectile",
        "neoorigins:set_block",
        "neoorigins:set_on_fire",
        "neoorigins:extinguish",
        "neoorigins:explode",
        "neoorigins:gain_air",
        "neoorigins:area_of_effect",
        "neoorigins:grant_power",
        "neoorigins:revoke_power",
        "neoorigins:trigger_cooldown",
        "neoorigins:execute_command",
        "neoorigins:play_sound",
        "neoorigins:swing_hand",
        "neoorigins:emit_game_event",
        "neoorigins:toggle",
        "neoorigins:add_to_set",
        "neoorigins:remove_from_set",
        "neoorigins:chain_to_nearest",
        "neoorigins:pull_entities",
        "neoorigins:throw_target",
        "neoorigins:swap_with_entity",
        "neoorigins:teleport_to_marker",
        "neoorigins:damage_attacker",
        "neoorigins:ignite_attacker",
        "neoorigins:effect_on_attacker",
        "neoorigins:random_teleport",
        "neoorigins:cancel_event",
        "neoorigins:kubejs_callback",
        "apace:damage",
        "apace:heal",
        "apace:feed",
        "apace:apply_effect",
        "apace:and",
        "apace:chance",
        "apace:give",
        "apace:execute_command",
        "apace:set_block",
        "apace:spawn_entity",
        "apace:spawn_projectile",
        "apace:area_of_effect",
        "neoorigins:and",
        "neoorigins:chance",
        "neoorigins:delay",
        "neoorigins:if_else",
        "neoorigins:if_else_list",
        "neoorigins:nothing",
        "neoorigins:damage",
        "neoorigins:heal",
        "neoorigins:feed",
        "neoorigins:exhaust",
        "neoorigins:change_resource",
        "neoorigins:apply_effect",
        "neoorigins:clear_effect",
        "neoorigins:add_velocity",
        "neoorigins:launch",
        "neoorigins:set_fall_distance",
        "neoorigins:dismount",
        "neoorigins:give",
        "neoorigins:modify_food",
        "neoorigins:spawn_entity",
        "neoorigins:spawn_projectile",
        "neoorigins:set_block",
        "neoorigins:set_on_fire",
        "neoorigins:extinguish",
        "neoorigins:explode",
        "neoorigins:gain_air",
        "neoorigins:area_of_effect",
        "neoorigins:grant_power",
        "neoorigins:revoke_power",
        "neoorigins:trigger_cooldown",
        "neoorigins:execute_command",
        "neoorigins:play_sound",
        "neoorigins:swing_hand",
        "neoorigins:emit_game_event"
      ]
    }
  },
  "oneOf": [
    {
      "$comment": "neoorigins:and / origins:if_else / origins:if_else_list \u00e2\u20ac\u201d combinators",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:and",
            "apace:and",
            "neoorigins:and"
          ]
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#"
          }
        }
      },
      "required": [
        "type",
        "actions"
      ]
    },
    {
      "$comment": "neoorigins:chance",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:chance",
            "apace:chance",
            "neoorigins:chance"
          ]
        },
        "chance": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Probability 0.0-1.0 (default 0.5)."
        },
        "action": {
          "$ref": "#",
          "description": "Action run when the roll succeeds."
        }
      },
      "required": ["type", "chance"]
    },
    {
      "$comment": "neoorigins:delay",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:delay",
            "neoorigins:delay"
          ]
        },
        "ticks": {
          "type": "integer",
          "minimum": 1
        },
        "action": {
          "$ref": "#"
        }
      },
      "required": [
        "type",
        "action"
      ]
    },
    {
      "$comment": "neoorigins:if_else",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:if_else",
            "neoorigins:if_else"
          ]
        },
        "condition": {
          "$ref": "condition.schema.json"
        },
        "if_action": {
          "$ref": "#"
        },
        "else_action": {
          "$ref": "#"
        }
      },
      "required": [
        "type",
        "condition"
      ]
    },
    {
      "$comment": "neoorigins:damage",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:damage",
            "apace:damage",
            "neoorigins:damage"
          ]
        },
        "amount": {
          "type": "number"
        },
        "source": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "properties": {
                "name": {
                  "type": "string"
                }
              },
              "required": [
                "name"
              ]
            }
          ]
        }
      },
      "required": [
        "type"
      ]
    },
    {
      "$comment": "neoorigins:heal / origins:exhaust / origins:gain_air â€” amount only (feed has its own branch; reads food/saturation, not amount).",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:heal",
            "neoorigins:exhaust",
            "neoorigins:gain_air",
            "apace:heal"
          ]
        },
        "amount": {
          "type": "number"
        }
      },
      "required": [
        "type",
        "amount"
      ]
    },
    {
      "$comment": "neoorigins:feed â€” reads food (int) + saturation (float), not amount.",
      "properties": {
        "type": {"enum": ["neoorigins:feed", "apace:feed"]},
        "food": {"type": "integer", "minimum": 0, "description": "Food points added (default 1)."},
        "saturation": {"type": "number", "minimum": 0, "description": "Saturation points added (default 0.0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:apply_effect",
      "properties": {
        "type": {"enum": ["neoorigins:apply_effect", "apace:apply_effect"]},
        "effect": {"type": "string", "description": "Single effect id (use this OR effects[])."},
        "effects": {"type": "array", "items": {"type": "object"}, "description": "Array of {effect, duration, amplifier, is_ambient, show_particles, show_icon} objects. First entry wins if both 'effect' and 'effects' are present."},
        "duration": {"type": "integer", "minimum": 1, "description": "Effect duration in ticks (default 200; honoured when using 'effect' scalar)."},
        "amplifier": {"type": "integer", "minimum": 0, "description": "Effect amplifier (default 0 = level I)."},
        "is_ambient": {"type": "boolean", "description": "Reduced particle visibility (default false)."},
        "show_particles": {"type": "boolean", "description": "Show effect particles (default true)."},
        "show_icon": {"type": "boolean", "description": "Show HUD icon (default true)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:clear_effect",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:clear_effect",
            "neoorigins:clear_effect"
          ]
        },
        "effect": {
          "type": "string"
        }
      },
      "required": [
        "type"
      ]
    },
    {
      "$comment": "neoorigins:add_velocity",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:add_velocity",
            "neoorigins:add_velocity"
          ]
        },
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        },
        "z": {
          "type": "number"
        },
        "set": {
          "type": "boolean"
        }
      },
      "required": [
        "type"
      ]
    },
    {
      "$comment": "neoorigins:launch",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:launch",
            "neoorigins:launch"
          ]
        },
        "speed": {
          "type": "number"
        }
      },
      "required": [
        "type"
      ]
    },
    {
      "$comment": "neoorigins:give",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:give",
            "apace:give",
            "neoorigins:give"
          ]
        },
        "stack": {
          "type": "object",
          "properties": {
            "item": {
              "type": "string"
            },
            "count": {
              "type": "integer",
              "minimum": 1
            }
          },
          "required": [
            "item"
          ]
        }
      },
      "required": [
        "type",
        "stack"
      ]
    },
    {
      "$comment": "neoorigins:execute_command",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:execute_command",
            "apace:execute_command",
            "neoorigins:execute_command"
          ]
        },
        "command": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "command"
      ]
    },
    {
      "$comment": "neoorigins:play_sound",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:play_sound",
            "neoorigins:play_sound"
          ]
        },
        "sound": {
          "type": "string"
        },
        "volume": {
          "type": "number"
        },
        "pitch": {
          "type": "number"
        }
      },
      "required": [
        "type",
        "sound"
      ]
    },
    {
      "$comment": "neoorigins:spawn_entity",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:spawn_entity",
            "apace:spawn_entity",
            "neoorigins:spawn_entity"
          ]
        },
        "entity_type": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "entity_type"
      ]
    },
    {
      "$comment": "neoorigins:spawn_projectile / fire_projectile",
      "properties": {
        "type": {"enum": ["neoorigins:spawn_projectile", "neoorigins:fire_projectile", "apace:spawn_projectile"]},
        "entity_type": {"type": "string", "description": "Projectile entity type id (e.g. minecraft:arrow)."},
        "projectile": {"type": "string", "description": "Alias for entity_type."},
        "speed": {"type": "number", "description": "Launch speed (default 1.5)."},
        "inaccuracy": {"type": "number", "minimum": 0, "description": "Random spread; 0 = perfect aim (default 0)."},
        "divergence": {"type": "number", "minimum": 0, "description": "Alias for inaccuracy."},
        "vertical_offset": {"type": "number", "description": "Spawn-Y offset from eye height (default 0)."},
        "on_hit_action": {"$ref": "#", "description": "Action run when the projectile hits something."},
        "projectile_action": {"$ref": "#", "description": "Alias for on_hit_action."},
        "effect_type": {"type": "string", "description": "Visual palette id for MagicOrb projectiles (client-side)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:area_of_effect",
      "properties": {
        "type": {"enum": ["neoorigins:area_of_effect", "apace:area_of_effect"]},
        "radius": {"type": "number", "exclusiveMinimum": 0, "description": "AoE radius in blocks."},
        "entity_action": {"$ref": "#", "description": "Action run on every caught entity."},
        "entity_condition": {"type": "object", "description": "Optional filter — only entities matching are hit."},
        "shape": {"enum": ["sphere", "cube"], "description": "AoE shape (default sphere)."},
        "include_source": {"type": "boolean", "description": "Include the caster in the AoE (default false)."}
      },
      "required": ["type", "radius"]
    },
    {
      "$comment": "neoorigins:grant_power / origins:revoke_power",
      "properties": {
        "type": {"enum": ["neoorigins:grant_power", "neoorigins:revoke_power"]},
        "power": {"type": "string", "description": "Power id to grant or revoke."},
        "power_id": {"type": "string", "description": "Alias for power."},
        "source": {"type": "string", "description": "Apoli-style 'source' label; honoured for parser compat but ignored by this implementation."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:toggle",
      "properties": {
        "type": {
          "const": "neoorigins:toggle"
        },
        "power": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "power"
      ]
    },
    {
      "$comment": "neoorigins:add_to_set / neoorigins:remove_from_set",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:add_to_set",
            "neoorigins:add_to_set",
            "neoorigins:remove_from_set",
            "neoorigins:remove_from_set"
          ]
        },
        "set": {
          "type": "string"
        }
      },
      "required": [
        "type",
        "set"
      ]
    },
    {
      "$comment": "neoorigins:nothing",
      "properties": {
        "type": {
          "enum": [
            "neoorigins:nothing",
            "neoorigins:nothing"
          ]
        }
      },
      "required": [
        "type"
      ]
    },
    {
      "$comment": "neoorigins:if_else_list",
      "properties": {
        "type": {"const": "neoorigins:if_else_list"},
        "actions": {"type": "array", "items": {"type": "object"}, "description": "List of {condition, action} branches; first matching condition wins."}
      },
      "required": ["type", "actions"]
    },
    {
      "$comment": "neoorigins:dash",
      "properties": {
        "type": {"const": "neoorigins:dash"},
        "strength": {"type": "number", "description": "Impulse magnitude along player look vector (default 1.5)."},
        "allow_vertical": {"type": "boolean", "description": "If false, dash is pinned to horizontal (default true)."},
        "set_velocity": {"type": "boolean", "description": "If true, replaces delta movement instead of pushing additively."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:exhaust",
      "properties": {
        "type": {"const": "neoorigins:exhaust"},
        "amount": {"type": "number", "minimum": 0, "description": "Exhaustion points added (default 1.0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:change_resource / modify_resource",
      "properties": {
        "type": {"enum": ["neoorigins:change_resource", "neoorigins:modify_resource"]},
        "resource": {"type": "string", "description": "Resource power id (matches the bar's defining power)."},
        "operation": {"enum": ["add", "set"], "description": "add (default) clamps within bar bounds; set assigns directly."},
        "change": {"type": "integer", "description": "Amount to add or value to set."}
      },
      "required": ["type", "resource"]
    },
    {
      "$comment": "neoorigins:trigger_cooldown",
      "properties": {
        "type": {"const": "neoorigins:trigger_cooldown"},
        "power": {"type": "string", "description": "Power id whose cooldown to start."},
        "cooldown": {"type": "integer", "minimum": 0, "description": "Cooldown duration in ticks (20 = 1s; default 20)."}
      },
      "required": ["type", "power"]
    },
    {
      "$comment": "neoorigins:gain_air",
      "properties": {
        "type": {"const": "neoorigins:gain_air"},
        "amount": {"type": "integer", "description": "Air-supply ticks to add, clamped to max (default 10)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:set_fall_distance",
      "properties": {
        "type": {"const": "neoorigins:set_fall_distance"},
        "fall_distance": {"type": "number", "description": "New fall distance value (default 0.0 \u2014 resets fall damage)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:extinguish",
      "properties": {"type": {"const": "neoorigins:extinguish"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:dismount",
      "properties": {"type": {"const": "neoorigins:dismount"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:explode",
      "properties": {
        "type": {"const": "neoorigins:explode"},
        "power": {"type": "number", "minimum": 0, "description": "Explosion strength (default 3.0; TNT = 4)."},
        "destruction_type": {"enum": ["none", "break", "destroy"], "description": "Block-interaction mode; 'none' is entity-only (default 'none')."},
        "create_fire": {"type": "boolean", "description": "Spawn fire on affected blocks (default false)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:set_block",
      "properties": {
        "type": {"const": "neoorigins:set_block"},
        "block": {"type": "string", "description": "Block id to place at the player's position."},
        "keep": {"type": "boolean", "description": "If true, only place when current block is air (default false)."}
      },
      "required": ["type", "block"]
    },
    {
      "$comment": "neoorigins:modify_food",
      "properties": {
        "type": {"const": "neoorigins:modify_food"},
        "food": {"type": "integer", "description": "Food points delta."},
        "food_component_food": {"type": "integer", "description": "Alias for food."},
        "saturation": {"type": "number", "description": "Saturation delta."},
        "food_component_saturation": {"type": "number", "description": "Alias for saturation."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:emit_game_event",
      "properties": {
        "type": {"const": "neoorigins:emit_game_event"},
        "event": {"type": "string", "description": "GameEvent id, e.g. minecraft:hit_ground."},
        "game_event": {"type": "string", "description": "Alias for event."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:swing_hand",
      "properties": {"type": {"const": "neoorigins:swing_hand"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:drop_items",
      "properties": {
        "type": {"const": "neoorigins:drop_items"},
        "mode": {"enum": ["each", "one_of", "single", "weighted"], "description": "'each' rolls every entry independently; 'one_of' picks one weighted by 'weight'."},
        "rolls": {"type": "integer", "minimum": 1, "description": "Number of weighted picks in one_of mode (default 1; ignored in each mode)."},
        "items": {"type": "array", "items": {"type": "object"}, "description": "Drop entries \u2014 each: {item, count (int|[min,max]), chance (each-mode), weight (one_of-mode)}."}
      },
      "required": ["type", "items"]
    },
    {
      "$comment": "neoorigins:spawn_lingering_area",
      "properties": {
        "type": {"const": "neoorigins:spawn_lingering_area"},
        "radius": {"type": "number", "description": "Effect cloud radius (default 3.0)."},
        "duration_ticks": {"type": "integer", "minimum": 1, "description": "Lifetime in ticks (default 100)."},
        "interval_ticks": {"type": "integer", "minimum": 1, "description": "Ticks between entity_action firings (default 20)."},
        "effect_type": {"type": "string", "description": "Visual effect palette id (client-side; optional)."},
        "particle_type": {"type": "string", "description": "Particle id rendered (default minecraft:witch)."},
        "entity_action": {"$ref": "#", "description": "Action run every interval against caught entities."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:spawn_black_hole",
      "properties": {
        "type": {"const": "neoorigins:spawn_black_hole"},
        "radius": {"type": "number", "description": "Pull radius (default 6.0)."},
        "duration_ticks": {"type": "integer", "minimum": 1, "description": "Lifetime (default 100)."},
        "pull_strength": {"type": "number", "description": "Per-tick pull magnitude (default 1.5)."},
        "damage_per_tick": {"type": "number", "description": "Damage applied per tick to caught entities (default 2.0)."},
        "effect_type": {"type": "string", "description": "Visual palette id (client-side; optional)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:spawn_tornado",
      "properties": {
        "type": {"const": "neoorigins:spawn_tornado"},
        "radius": {"type": "number", "description": "Vortex radius (default 5.0)."},
        "duration_ticks": {"type": "integer", "minimum": 1, "description": "Lifetime (default 100)."},
        "pull_strength": {"type": "number", "description": "Horizontal pull magnitude (default 1.0)."},
        "lift_strength": {"type": "number", "description": "Upward lift magnitude (default 0.5)."},
        "spin_strength": {"type": "number", "description": "Tangential spin magnitude (default 0.5)."},
        "damage_per_interval": {"type": "number", "description": "Damage per interval (default 2.0)."},
        "damage_interval_ticks": {"type": "integer", "minimum": 1, "description": "Ticks between damage applications (default 10)."},
        "effect_type": {"type": "string", "description": "Visual palette id (client-side; optional)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:chain_to_nearest",
      "properties": {
        "type": {"const": "neoorigins:chain_to_nearest"},
        "radius": {"type": "number", "description": "Search radius (default 16)."},
        "speed": {"type": "number", "description": "Pull speed (default 1.0)."},
        "target_condition": {"$ref": "#", "description": "Optional filter \u2014 only entities matching are valid targets."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:pull_entities",
      "properties": {
        "type": {"const": "neoorigins:pull_entities"},
        "radius": {"type": "number", "description": "Pull radius (default 8)."},
        "strength": {"type": "number", "description": "Pull magnitude per entity (default 0.5)."},
        "include_players": {"type": "boolean", "description": "Whether to pull other players (default true)."},
        "entity_condition": {"$ref": "#", "description": "Optional filter on which entities to pull."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:throw_target",
      "properties": {
        "type": {"const": "neoorigins:throw_target"},
        "force": {"type": "number", "description": "Horizontal throw magnitude (default 1.5)."},
        "vertical_lift": {"type": "number", "description": "Upward push (default 0.5)."},
        "max_distance": {"type": "number", "description": "Raycast distance to find target under crosshair (default 5.0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:swap_with_entity",
      "properties": {
        "type": {"const": "neoorigins:swap_with_entity"},
        "radius": {"type": "number", "description": "Search radius for nearest target (default 16)."},
        "target_condition": {"$ref": "#", "description": "Optional filter on valid swap targets."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:teleport_to_marker",
      "properties": {
        "type": {"const": "neoorigins:teleport_to_marker"},
        "dx": {"type": "number", "description": "Relative X offset (used when no 'position' object)."},
        "dy": {"type": "number", "description": "Relative Y offset."},
        "dz": {"type": "number", "description": "Relative Z offset."},
        "position": {"type": "object", "description": "Absolute target \u2014 {x, y, z} object overrides dx/dy/dz."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:damage_attacker",
      "properties": {
        "type": {"const": "neoorigins:damage_attacker"},
        "amount": {"type": "number", "description": "Flat damage (default 2.0; ignored if amount_ratio set)."},
        "amount_ratio": {"type": "number", "description": "Ratio of incoming hit amount (e.g. 0.25 = 25% reflected)."},
        "source": {"type": "object", "description": "Damage source \u2014 {name: fire|lava|magic|generic|...}."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:ignite_attacker",
      "properties": {
        "type": {"const": "neoorigins:ignite_attacker"},
        "ticks": {"type": "integer", "minimum": 0, "description": "Fire duration on attacker (default 60)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:effect_on_attacker",
      "properties": {
        "type": {"const": "neoorigins:effect_on_attacker"},
        "effect": {"type": "string", "description": "Mob-effect id to apply."},
        "duration": {"type": "integer", "minimum": 1, "description": "Effect duration in ticks (default 100)."},
        "amplifier": {"type": "integer", "minimum": 0, "description": "Effect amplifier (default 0 = level I)."}
      },
      "required": ["type", "effect"]
    },
    {
      "$comment": "neoorigins:random_teleport",
      "properties": {
        "type": {"const": "neoorigins:random_teleport"},
        "horizontal_range": {"type": "number", "minimum": 0, "description": "X/Z search radius (default 16)."},
        "range": {"type": "number", "minimum": 0, "description": "Alias for horizontal_range."},
        "vertical_range": {"type": "number", "minimum": 0, "description": "Y search radius (default 8)."},
        "attempts": {"type": "integer", "minimum": 1, "description": "Random-pick attempts before giving up (default 16)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:cancel_event",
      "properties": {"type": {"const": "neoorigins:cancel_event"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:toggle",
      "properties": {
        "type": {"const": "neoorigins:toggle"},
        "power": {"type": "string", "description": "Toggle power id to flip (or set if 'value' provided)."},
        "value": {"type": "boolean", "description": "Optional explicit state \u2014 omit to flip current state."}
      },
      "required": ["type", "power"]
    },
    {
      "$comment": "neoorigins:add_to_set / remove_from_set",
      "properties": {
        "type": {"enum": ["neoorigins:add_to_set", "neoorigins:remove_from_set"]},
        "set": {"type": "string", "description": "Named entity-set key on the actor (namespace it, e.g. mypack:hit_streak)."}
      },
      "required": ["type", "set"]
    },
    {
      "$comment": "neoorigins:target_action / actor_action",
      "properties": {
        "type": {"enum": ["neoorigins:target_action", "neoorigins:actor_action"]},
        "action": {"$ref": "#", "description": "Inner action to delegate to (target or actor; bientity unwrappers)."}
      },
      "required": ["type", "action"]
    },
    {
      "$comment": "neoorigins:block_action_at",
      "properties": {
        "type": {"const": "neoorigins:block_action_at"},
        "block_action": {"type": "object", "description": "Block action run at the entity's block position; ~ ~ ~ resolves to that block."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:choice",
      "properties": {
        "type": {"const": "neoorigins:choice"},
        "actions": {"type": "array", "items": {"type": "object"}, "description": "List of {action, weight} entries; one runs per dispatch, weighted."}
      },
      "required": ["type", "actions"]
    },
    {
      "$comment": "neoorigins:mount",
      "properties": {
        "type": {"const": "neoorigins:mount"},
        "entity_type": {"type": "string", "description": "Entity id to mount (e.g. minecraft:pig); falls back to any nearest mountable."},
        "radius": {"type": "number", "minimum": 0, "description": "Search radius (default 5.0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:passenger_action",
      "properties": {
        "type": {"const": "neoorigins:passenger_action"},
        "action": {"$ref": "#", "description": "Action run on each player passenger (alias: entity_action)."},
        "entity_action": {"$ref": "#", "description": "Same as 'action' \u2014 Apoli accepts both."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:spawn_effect_cloud",
      "properties": {
        "type": {"const": "neoorigins:spawn_effect_cloud"},
        "effect": {"type": "string", "description": "Effect id to apply (or an object {effect, duration, amplifier})."},
        "duration": {"type": "integer", "minimum": 1, "description": "Cloud lifetime + effect duration in ticks (default 200)."},
        "amplifier": {"type": "integer", "minimum": 0, "description": "Effect amplifier (default 0)."},
        "radius": {"type": "number", "minimum": 0, "description": "Cloud radius (default 3.0)."},
        "wait_time": {"type": "integer", "minimum": 0, "description": "Ticks before the cloud starts applying effects (default 10)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:offset",
      "properties": {
        "type": {"const": "neoorigins:offset"},
        "x": {"type": "number", "description": "X offset for the inner action's positional context."},
        "y": {"type": "number", "description": "Y offset."},
        "z": {"type": "number", "description": "Z offset."},
        "action": {"$ref": "#", "description": "Inner action \u2014 typically a block_action that reads position from context."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:add_xp",
      "properties": {
        "type": {"const": "neoorigins:add_xp"},
        "points": {"type": "integer", "description": "Raw experience points to grant (default 0)."},
        "levels": {"type": "integer", "description": "Experience levels to grant (default 0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:crafting_table",
      "properties": {"type": {"const": "neoorigins:crafting_table"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:invert",
      "properties": {"type": {"const": "neoorigins:invert"}},
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:set_on_fire",
      "properties": {
        "type": {"const": "neoorigins:set_on_fire"},
        "ticks": {"type": "integer", "minimum": 0, "description": "Fire duration in ticks (default 20 = 1s)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:equipped_item_action",
      "properties": {
        "type": {"const": "neoorigins:equipped_item_action"},
        "equipment_slot": {"enum": ["mainhand", "offhand", "head", "chest", "legs", "feet"], "description": "Equipment slot to read the stack from (default mainhand)."},
        "action": {"type": "object", "description": "ItemAction to run on the stack (consume, damage, set-NBT, etc.)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:modify_inventory",
      "properties": {
        "type": {"const": "neoorigins:modify_inventory"},
        "item_condition": {"type": "object", "description": "Optional ItemCondition filtering which stacks to process."},
        "item_action": {"type": "object", "description": "ItemAction run on each matching stack."},
        "process_mode": {"enum": ["items", "stacks"], "description": "items (default) counts individual items; stacks counts whole stacks toward the limit."},
        "limit": {"type": "integer", "minimum": 0, "description": "Cap on items/stacks processed; 0 = no limit (default 0)."}
      },
      "required": ["type"]
    },
    {
      "$comment": "neoorigins:raycast",
      "properties": {
        "type": {"const": "neoorigins:raycast"},
        "distance": {"type": "number", "exclusiveMinimum": 0, "description": "Max ray length in blocks (default 10)."},
        "block": {"type": "boolean", "description": "Test for block collisions (default true)."},
        "entity": {"type": "boolean", "description": "Test for entity collisions (default false)."},
        "fluid_handling": {"enum": ["none", "source_only", "any"], "description": "How fluids are treated (default none)."},
        "block_action": {"$ref": "#", "description": "Action when a block is hit; ~ ~ ~ resolves to the hit block."},
        "bientity_action": {"$ref": "#", "description": "Action when an entity is hit (with the hit entity as target context)."},
        "miss_action": {"$ref": "#", "description": "Action when nothing is hit within range."}
      },
      "required": ["type"]
    },
    {
      "$comment": "Fallback \u00e2\u20ac\u201d permissive for verbs not yet schema'd.",
      "properties": {
        "type": {
          "not": {
            "enum": [
              "neoorigins:and",
              "apace:and",
              "neoorigins:chance",
              "apace:chance",
              "neoorigins:delay",
              "neoorigins:if_else",
              "neoorigins:damage",
              "apace:damage",
              "neoorigins:heal",
              "neoorigins:feed",
              "neoorigins:exhaust",
              "neoorigins:gain_air",
              "apace:heal",
              "apace:feed",
              "neoorigins:apply_effect",
              "apace:apply_effect",
              "neoorigins:clear_effect",
              "neoorigins:add_velocity",
              "neoorigins:launch",
              "neoorigins:give",
              "apace:give",
              "neoorigins:execute_command",
              "apace:execute_command",
              "neoorigins:play_sound",
              "neoorigins:spawn_entity",
              "apace:spawn_entity",
              "neoorigins:spawn_projectile",
              "apace:spawn_projectile",
              "neoorigins:area_of_effect",
              "apace:area_of_effect",
              "neoorigins:grant_power",
              "neoorigins:revoke_power",
              "neoorigins:toggle",
              "neoorigins:add_to_set",
              "neoorigins:add_to_set",
              "neoorigins:remove_from_set",
              "neoorigins:remove_from_set",
              "neoorigins:nothing",
              "neoorigins:and",
              "neoorigins:chance",
              "neoorigins:delay",
              "neoorigins:if_else",
              "neoorigins:damage",
              "neoorigins:heal",
              "neoorigins:feed",
              "neoorigins:exhaust",
              "neoorigins:gain_air",
              "neoorigins:apply_effect",
              "neoorigins:clear_effect",
              "neoorigins:add_velocity",
              "neoorigins:launch",
              "neoorigins:give",
              "neoorigins:execute_command",
              "neoorigins:play_sound",
              "neoorigins:spawn_entity",
              "neoorigins:spawn_projectile",
              "neoorigins:area_of_effect",
              "neoorigins:grant_power",
              "neoorigins:revoke_power",
              "neoorigins:nothing"
            ]
          }
        }
      },
      "additionalProperties": true
    },
    {
      "$comment": "neoorigins:kubejs_callback",
      "properties": {
        "type": {"const": "neoorigins:kubejs_callback"},
        "id": {"type": "string", "description": "ID of the JS callback to invoke. Register from JS via NeoOrigins.registerCallback(id, fn)."}
      },
      "required": ["type", "id"]
    }
  ]
}