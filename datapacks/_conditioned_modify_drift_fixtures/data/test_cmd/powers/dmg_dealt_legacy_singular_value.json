{
  "type": "origins:modify_damage_dealt",
  "name": "Strong Arm (legacy NeoOrigins shape: singular modifier + value)",
  "description": "Pre-existing shape the old parser accepted; must keep working post-fix (backwards-compatible). `condition` present so this routes through parseConditionedModifyDamageDealt (Route B fallback).",
  "condition": { "type": "origins:in_block", "block_condition": { "type": "origins:in_tag", "tag": "minecraft:water" } },
  "modifier": { "operation": "multiply_base", "value": 0.5 }
}
